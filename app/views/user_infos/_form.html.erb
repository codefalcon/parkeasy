<%= form_for(@user_info) do |f| %>
  <% if @user_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_info.errors.count, "error") %> prohibited this user_info from being saved:</h2>

      <ul>
      <% @user_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :proof_type_id %><br />
    <%= f.text_field :proof_type_id %>
  </div>
  <div class="field">
    <%= f.label :proof_id %><br />
    <%= f.text_field :proof_id %>
  </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
